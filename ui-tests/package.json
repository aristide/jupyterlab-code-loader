{
  "name": "jupyterlab-code-loader-ui-tests",
  "version": "1.0.0",
  "description": "JupyterLab jupyterlab-code-loader Integration Tests",
  "private": true,
  "scripts": {
    "start": "jupyter lab --config jupyter_server_test_config.py",
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
